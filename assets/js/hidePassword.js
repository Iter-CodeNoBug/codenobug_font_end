const hidePasswdList = document.getElementsByClassName("fa-eye");
const inputPassElementLogin = document.getElementById("login-input-passwd");
const inputPassElementSignup = document.getElementById("signup-input-passwd");
const inputRepassElementSignup = document.getElementById("signup-input-repasswd");

for (let hidePasswdItem of hidePasswdList) {
    hidePasswdItem.addEventListener("click", function() {
        
    });
}